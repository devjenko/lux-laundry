import 'flowbite';

// Initialize Flowbite components after DOM is loaded
document.addEventListener('DOMContentLoaded', function() {
    // Flowbite is automatically initialized, but you can manually reinitialize if needed
    // window.initFlowbite();
});
